package com.bccs.gatewaymanager.entity;

/** Loai thay doi tao ra 1 EndpointConfigVersion - hien thi tren UI lich su phien ban. */
public enum EndpointChangeType {
    CREATED, UPDATED, ROLLED_BACK
}
