package com.bccs.gatewaymanager.entity;

/** Toan tu so sanh dung cho dieu kien re nhanh cua 1 BackendStep - xem CompositeOrchestratorEngine.evaluateCondition(). */
public enum ConditionOperator {
    EQUALS, NOT_EQUALS, EXISTS, NOT_EXISTS
}
