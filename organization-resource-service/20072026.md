# 20/07/2026 - Ngày làm việc

## Nguồn dữ liệu
- File SQL DDL: `E:\product_4.0_new\organization-resource-service\sql`

---

## 1. Module Shop

### Entity
- `src/.../shop/entity/ShopEntity.java` — 59 cột, `@Table(name = "SHOP")`, `@Lob @Basic(fetch = FetchType.LAZY)` cho `FILE_ATTACH`

### DTOs
- `shop/dto/request/ShopCreateRequest.java` — record (tất cả cột trừ read-only)
- `shop/dto/request/ShopUpdateRequest.java` — record (tất cả cột trừ read-only, có `shopId`)
- `shop/dto/request/StockCodeRequest.java` — record `(Long ownerId, Integer ownerType)`
- `shop/dto/response/ShopResponse.java` — record 59 cột
- `shop/dto/response/StockCodeResponse.java` — record `(String stockCode)`

### Mapper
- `shop/mapper/ShopMapper.java` — `toEntity()`, `updateEntity()`, `toResponse()`

### Repository
- `shop/repository/ShopRepository.java` — `JpaRepository`, `findByShopIdAndStatus(Long, Integer)`, `findByShopCode`, `existsByShopCode`

### Service
- `shop/service/ShopService.java`:
  - `getById(Long shopId)` — trả bản ghi có `status = 1`, throw `BusinessException("ORGANIZATION-SHOP-002")` nếu không tìm thấy
  - `getStockCode(Long ownerId, Integer ownerType)` — ownerType=1 → lookup Shop → return shopCode; ownerType=2 → lookup Staff → return staffCode; throw `ORGANIZATION-SHOP-003` nếu ownerType không hợp lệ

### Controller
- `shop/controller/ShopController.java`:
  - `GET /api/v1/shops/{shopId}` — gọi `shopService.getById()`
  - `GET /api/v1/shops/stock-code?ownerId=&ownerType=` — gọi `shopService.getStockCode()`

---

## 2. Module Staff

### Entity
- `src/.../staff/entity/StaffEntity.java` — 60 cột, `@Table(name = "STAFF")`, `@Lob @Basic(fetch = FetchType.LAZY)` cho `FILE_ATTACH`

### DTOs
- `staff/dto/request/StaffCreateRequest.java` — record
- `staff/dto/request/StaffUpdateRequest.java` — record (có `staffId`)
- `staff/dto/response/StaffResponse.java` — record 60 cột

### Mapper
- `staff/mapper/StaffMapper.java` — `toEntity()`, `updateEntity()`, `toResponse()`

### Repository
- `staff/repository/StaffRepository.java` — chỉ có `findByStaffIdAndStatus(Long, Integer)`

### Service
- `staff/service/StaffService.java`:
  - `getById(Long staffId)` — trả bản ghi có `status = 1`, throw `BusinessException("ORGANIZATION-STAFF-002")` nếu không tìm thấy

### Controller
- `staff/controller/StaffController.java`:
  - `GET /api/v1/staffs/{staffId}` — gọi `staffService.getById()`

---

## 3. Cross-module Dependency
- `ShopService` inject `StaffRepository` để phục vụ `getStockCode` khi `ownerType=2`

---

## 4. Error Codes
| Code | Mô tả |
|------|--------|
| ORGANIZATION-SHOP-002 | Shop not found |
| ORGANIZATION-STAFF-002 | Staff not found |
| ORGANIZATION-SHOP-003 | Invalid ownerType |

---

## 5. Postman Collection
- `postman/OrganizationResourceService.postman_collection.json`
- 3 request: Get Shop by ID, Get Stock Code, Get Staff by ID
- Base URL: `http://localhost:8004/api/v1`

---

## 6. Config (organization-resource-service)
- Default port: **8004** (`application.yml`)
- Local profile port: **8080** (`application-local.yml`)
- Compile: OK

---

## 7. product-catalog-service — Module Product

### Nguồn dữ liệu
- File SQL: `E:\product_4.0_new\product-catalog-service\catalog-sql`

### Entity
- `src/.../product/entity/ProductOfferingEntity.java` — 42 cột, `@Table(name = "PRODUCT_OFFERING")`

### DTOs
- `product/dto/response/ProductOfferingResponse.java` — record 42 cột

### Mapper
- `product/mapper/ProductOfferingMapper.java` — `toResponse()`

### Repository
- `product/repository/ProductOfferingRepository.java` — `findByCode(String code)`

### Service
- `product/service/ProductOfferingService.java`:
  - `getByProductCode(String productCode)` — lookup theo code, trả full response, throw `BusinessException("CATALOG-PRODUCT-001")` nếu không tìm thấy

### Controller
- `product/controller/ProductOfferingController.java`:
  - `GET /api/v1/products?productCode=CODE` — gọi `productOfferingService.getByProductCode()`

### Postman
- `postman/ProductCatalogService.postman_collection.json`
- Base URL: `http://localhost:8001/api/v1`

### Config
- Port: **8001** (both default và local profile)
- Service started, API tested — trả 500 `CATALOG-PRODUCT-001` khi không tìm thấy (expected)